<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bit.mapper.UserMapper">
	<select id ="getList" resultType="com.bit.domain.UserVO">
		<![CDATA[
		select * from member where seq > 0
		]]>
	</select>
	
	<insert id="insert">
		insert into member(seq, id, password, name, phone, email, address, sex) values (seq_member.nextval,#{id},#{password},#{name},#{phone},#{email},#{address},#{sex})
	</insert>
	
	<insert id="insertSelectKey">
		<selectKey keyProperty="seq" order="BEFORE" resultType="long">
			select seq_board.nextval from dual
		</selectKey>
		
		insert into member(seq, id, password, name, phone, email, address, sex) values (seq_member.nextval,#{id},#{password},#{name},#{phone},#{email},#{address},#{sex})
	</insert>
	
	<select id="read" resultType="com.bit.domain.UserVO">
		select * from member where seq = #{seq}
	</select>
	
	<delete id="delete">
		delete member where seq = #{seq}
	</delete>
	
	<update id="update">
		update member set
		id = #{id},
		password = #{password},
		name = #{name},
		phone = #{phone},
		email = #{email},
		address = #{address},
		sex = #{sex},
		updatedate = sysdate
		where seq = #{seq}
	</update>
</mapper>
